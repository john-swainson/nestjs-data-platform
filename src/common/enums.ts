export enum Gender {
  FEMALE = 'FEMALE',
  MALE = 'MALE',
};

export enum SortField {
  GENDER = 'GENDER',
  DOB = 'DOB',
  LAST_NAME = 'LAST_NAME',
};

export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
};
